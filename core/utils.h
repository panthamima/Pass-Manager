#ifndef PM_UTILS_H
#define PM_UTILS_H

char* removeXChar(char* str);       // удаление запрещенного символа из массива
void reverse(char s[]);             // reverse string
void iToc(int num, char str[]);     // int -> char
int  counting();                    // подсчет строк


#endif